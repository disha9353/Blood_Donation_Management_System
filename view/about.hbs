<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"
    integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz"
    crossorigin="anonymous"></script>
  <script src="https://cdnjs.cloudflare.com/ajax/libs/gsap/3.12.2/gsap.min.js"></script>
  <script src="https://cdnjs.cloudflare.com/ajax/libs/gsap/3.12.2/ScrollTrigger.min.js"></script>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet"
    integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
  <link rel="stylesheet" href="/css/about.css">
  <title>About</title>
</head>

<body>

  <nav class="nav">
    <div class="nav-links">
      <a href="/">Home</a>
      <a href="/latest">Latest</a>
      <a href="/about">About</a>
      <a href="/contact">Contact</a>
      <a href="/register">Register</a>
      <a href="/admin-login">Admin Login</a>
    </div>
  </nav>

  <div class="floating-shapes">
    <div id="shape1">⬡</div>
    <div id="shape2">◆</div>
    <div id="shape3">⬢</div>
  </div>

  {{!-- <section class="hero" id="home">
    <div class="card">
      <h1>Vidwath Dev</h1>
      <p>Web Designer & Backend Developer<br>
        Crafting digital experiences that leave an impression</p>
      <div class="social-links">
        <a href="https://vidwath-dev.github.io/vidwathdevk.in/" class="btn">Portfolio</a>
        <a href="https://www.linkedin.com/in/vidwath-dev-k-14348a309" class="btn">Contact</a>
      </div>
    </div>
  </section> --}}
  <section>
    <div class="container">
      <h1 style="text-align: center;">About Blood Management</h1>
      <h5 style="margin-top: 40px;">Blood management is essential for the success of any healthcare system. It involves tracking blood donations, inventory levels, patient requests, and safe distribution.</h5>
    </div>
  </section>
  <section class="skills" id="skills" style="margin-top: -60px;">
    <div class="container">
      <h2>Why is Blood Management Important?</h2>
      <h6>Effective blood management helps to:</h6>
      <div class="skill-grid">
        <div class="skill-card">
          <h3>Ensure timely availability of blood for patients in need</h3>
          {{!-- <p>Reduce costs by minimizing excess stock.</p> --}}
        </div>
        <div class="skill-card">
          <h3>Prevent wastage by tracking blood expiry and usage</h3>
          {{!-- <p>Designing intuitive and beautiful user interfaces</p> --}}
        </div>
        <div class="skill-card">
          <h3>Improve emergency response through real-time inventory monitoring</h3>
          {{!-- <p>Bringing ideas to life with stunning 3D animations</p> --}}
        </div>
        <div class="skill-card">
          <h3>Enhance donor coordination and optimize donation drives</h3>
          {{!-- <p>Bringing ideas to life with stunning 3D animations</p> --}}
        </div>
      </div>
    </div>
  </section>


  <section class="skills" id="skills" style="margin-top: -60px;">
    <div class="container">
      <h2>Features of Our Blood Management System</h2>
      <h6>Our system offers</h6>
      <div class="skill-grid">
        <div class="skill-card">
          <h3>Donar-Patient Connectivity</h3>
          {{!-- <p>Reduce costs by minimizing excess stock.</p> --}}
        </div>
        <div class="skill-card">
          <h3>Smart Matching Engine</h3>
          {{!-- <p>Designing intuitive and beautiful user interfaces</p> --}}
        </div>
        <div class="skill-card">
          <h3>Multi-User Access & Roles</h3>
          {{!-- <p>Bringing ideas to life with stunning 3D animations</p> --}}
        </div>
        <div class="skill-card">
          <h3>Donor & Patient History</h3>
          {{!-- <p>Bringing ideas to life with stunning 3D animations</p> --}}
        </div>
      </div>
    </div>
  </section>


  <script>
    // 3D Card Effect
    document.addEventListener('mousemove', (e) => {
      const cards = document.querySelectorAll('.card');
      cards.forEach(card => {
        const rect = card.getBoundingClientRect();
        const x = e.clientX - rect.left;
        const y = e.clientY - rect.top;

        const centerX = rect.width / 2;
        const centerY = rect.height / 2;

        const rotateX = (y - centerY) / 20;
        const rotateY = -(x - centerX) / 20;

        card.style.transform = `rotateX(${rotateX}deg) rotateY(${rotateY}deg)`;
      });
    });

    // Reset card rotation when mouse leaves
    document.addEventListener('mouseleave', () => {
      const cards = document.querySelectorAll('.card');
      cards.forEach(card => {
        card.style.transform = 'rotateX(0) rotateY(0)';
      });
    });

    // Smooth scroll for navigation
    document.querySelectorAll('nav a').forEach(anchor => {
      anchor.addEventListener('click', function (e) {
        const targetId = this.getAttribute('href');

        // Check if the link starts with "#" (indicating an on-page target)
        if (targetId.startsWith('#')) {
          e.preventDefault();
          document.querySelector(targetId).scrollIntoView({
            behavior: 'smooth'
          });
        }
      });

    });

    // Parallax effect for floating shapes
    window.addEventListener('scroll', () => {
      const shapes = document.querySelectorAll('.floating-shapes div');
      const scrolled = window.pageYOffset;

      shapes.forEach((shape, index) => {
        const speed = (index + 1) * 0.2;
        shape.style.transform = `translateY(${scrolled * speed}px)`;
      });
    });
  </script>

</body>

</html>